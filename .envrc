# Add bazel-tools and tools to PATH
PATH_add bazel-tools
PATH_add tools

# Helper to ensure toolchain is set up
if [ ! -d "bazel-tools" ] || [ ! -x "bazel-tools/clang" ]; then
    echo "Tip: Run ./tools/setup_toolchain.py to initialize the toolchain binaries."
fi
